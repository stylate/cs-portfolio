import React from 'react';
import styled from 'styled-components';

const Text = styled.h2`
    font-size: 48px;
    font-family: 'EB Garamond', sans-serif;
    padding-left: 1.3em;
    padding-top: 5em;
    opacity: 0.9;
    font-weight: 100;
    letter-spacing: 1.2px;
`

const Header = (
    <Text>alan nguyen</Text>
);

export default Header;